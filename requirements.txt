# Pin versions. A scikit-learn pickle must be loaded by the SAME minor version
# that created it, or you get warnings and sometimes silently wrong behaviour.
# Check yours in Colab with:  import sklearn; print(sklearn.__version__)
Flask==3.1.3
scikit-learn==1.6.0
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0
gunicorn==23.0.0
